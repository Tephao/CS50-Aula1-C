#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    long cartao;

    do
    {
        cartao = get_long("\n\nQual o número do cartão? -APENAS NÚMEROS : ");

    } while(cartao<0);


    if(cartão)
    for(int i=0;i>)
}